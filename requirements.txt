# SehatScan Core Dependencies for Streamlit Cloud
streamlit>=1.39.0
plotly>=5.24.0
huggingface-hub>=0.24.0
python-dotenv>=1.0.1
pillow>=10.0.0
pandas>=2.1.0
numpy>=1.24.0
requests>=2.31.0
google-generativeai>=0.8.0

# Note: OCR dependencies (PaddleOCR) are not compatible with Streamlit Cloud
# The app will automatically use JSON-only mode
# For local development with OCR, use: uv sync --extra ocr